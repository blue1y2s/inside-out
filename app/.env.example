# Environment Variables for SeeYourself App

# Server-side OpenAI API Key (optional - app works without it using rule-based analysis)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# Optional: override the upstream base URL (default https://api.openai.com/v1)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: comma-separated allowed origins for the proxy
ALLOWED_ORIGINS=http://localhost:3000

# Optional: client-side API base URL for production (leave empty for same-origin)
# VITE_API_BASE_URL=https://your-domain.example
